apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: medconnect
  labels:
    app.kubernetes.io/name: medconnect
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: medconnect
    app.kubernetes.io/component: backend
  ports:
    - name: http
      port: 8080
      targetPort: http
